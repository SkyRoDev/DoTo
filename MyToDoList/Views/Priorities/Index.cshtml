@model IEnumerable<MyToDoList.Priorities>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descryption)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descryption)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PriorityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PriorityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PriorityId })
        </td>
    </tr>
}

</table>
